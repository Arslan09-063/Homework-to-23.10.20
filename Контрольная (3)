//Arslan Sadovinov: 09-063. Самостотельная работа for Islamurov Islam Rinatovich 14.12.20.
//Контрольная. №3.

#include <iostream>
#include <cmath>

using namespace std;

int main()
{
	int i, n, a;
	double x;
	bool t;
	a = 0;
	x = 0;
	t = false;
	cin >> n;
	int* a = new int[n];
	for (i = 0; i < n; i++)
	{
		cin >> a[i];
		if (a[i] %2!= 0)
		t = true;
	}
	if (t)
	{
		for (i = 0; i < n; i++)

			if (a[i] % 2 != 0)
			{
				x = x + a[i];
				c++;
	    }
		x = x / a;
		cout.precision(3);
		cout << x;
    	}
	  else
		cout << "NO";
	return 0;
	}
